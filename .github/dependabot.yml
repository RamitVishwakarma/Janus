version: 2
updates:
  # The app itself has no dependencies; this keeps the workflow actions current.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "ci"
